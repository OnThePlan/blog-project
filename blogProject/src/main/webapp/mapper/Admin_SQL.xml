<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="admin">
	<!-- 공지사항 리스트 -->
	<select id="getNoticeList" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT notice_no, notice_title, notice_cnt, M_NO, 
			DATE_FORMAT(create_date,'%Y-%m-%d') AS create_date, 
			DATE_FORMAT(update_date,'%Y-%m-%d') AS update_date
			FROM notice WHERE delet_yn='N' ORDER BY update_date DESC
		]]>
	</select>

</mapper>
